[project]
name = "fridgesheet"
version = "0.4.0"
description = "Fridge Sheet: a parent's daily open-work sheet from Canvas + Home Access Center, a browser app, and a local MCP server for Claude."
requires-python = ">=3.11"
license = "MIT"
license-files = ["LICENSE"]
readme = "README.md"
dependencies = [
    "fastapi>=0.115",
    "jinja2>=3.1",
    "mcp>=1.2,<3",
    "playwright>=1.45",
    "python-dotenv>=1.0",
    "python-multipart>=0.0.9",
    "reportlab>=4",
    "segno>=1.6",
    "tomli-w>=1.0",
    "tzdata>=2024.1; sys_platform == 'win32'",
    "uvicorn>=0.30",
]

[project.optional-dependencies]
dev = ["pytest>=8", "httpx>=0.27"]
windows = ["keyring>=25", "pywin32>=306"]

[project.scripts]
fridgesheet = "fridgesheet.cli:main"
# The command's old name, kept for one release (fridgesheet.cli.legacy_main prints a notice).
lakota-grades = "fridgesheet.cli:legacy_main"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
filterwarnings = [
    # starlette.testclient's own module-level type annotation touches the deprecated
    # anyio.abc.BlockingPortal alias; nothing in this repo imports it directly, and the
    # installed starlette (1.6.0, latest at time of writing) has not been updated yet.
    "ignore:The anyio\\.abc\\.BlockingPortal alias is deprecated.*:DeprecationWarning:starlette\\.testclient",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["fridgesheet*"]

[tool.setuptools.package-data]
fridgesheet = ["host/*.xml", "web/templates/*.html", "web/static/*"]
